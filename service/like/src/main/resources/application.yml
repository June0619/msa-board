server:
  port: 9002
spring:
  application:
    name: jwjung-board-like-service
  datasource:
    url: jdbc:mysql://localhost:3306/article_like
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none